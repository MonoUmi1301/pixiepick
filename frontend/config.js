// ============================================================
//  config.js — แก้ไขแค่ไฟล์นี้ไฟล์เดียวเมื่อ deploy
// ============================================================

const CONFIG = {
  // เปลี่ยนเป็น URL ของ Railway server เมื่อ deploy
  // ตัวอย่าง: "https://pixelstore-server.up.railway.app"
  API_URL: "http://127.0.0.1:8888"
};
